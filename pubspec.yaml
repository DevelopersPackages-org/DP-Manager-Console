name: DPManagerConsole
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

#dependencies:
#  path: ^1.7.0

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
dependencies:
  args: ^1.6.0
  pub_sub: ^2.3.0
  firebase_node: ^0.1.3+1

executables:
  dpManager:
  dpm: